# 남자방(NamJaBang) 팀 개발 계획서

> 대상: 4인 팀, 1학기(12주) MVP 개발
> 전제: 팀장(본인)만 백엔드(Spring Boot) 경험 보유, Flask는 처음 사용. 나머지 3명은 개발 자체가 처음.
> 배포 범위: 실제 호스팅 없이 **로컬 실행 시연**까지.
> 크롤러: 기획서상 "수동/배치 적재"이지만, 본 계획에서는 **네이버 부동산 실제 크롤러**를 MVP 범위로 구현.
> 이 문서는 `planning_spec.md` / `feature_spec.md` / `architecture_spec.md` / `erd_spec.md` / `api_spec.md`를 전제로 작성됨.

---

## 0. 협업 규칙

- **브랜치 전략**: `main`(발표/데모용 안정 버전) — `develop`(통합) — `feature/기능명`(개인 작업). `feature` → `develop`은 PR + 최소 1인 리뷰 필수. `develop` → `main`은 마일스톤(주차 말) 단위로만 병합.
- **코드 리뷰**: 백엔드 PR은 우선적으로 팀장이 리뷰 겸 멘토링(초보 팀원 온보딩 목적). 프론트 PR은 AI 생성 코드라도 실행 확인 후 병합.
- **커뮤니케이션**: 주 1회 정기 회의(진행상황 + 막힌 점 공유) + 카톡/디스코드 상시 채널. 이슈/할일은 GitHub Issues 또는 Notion 중 택1로 통일.
- **코드 컨벤션**: 백엔드는 PEP8 + Black 포매터, 커밋 메시지는 `feat/fix/docs/refactor: 설명` 형식.
- **환경 통일**: Python 버전, Node 버전, `.env.example` 파일을 1주차에 확정해 전원 동일 환경 구성.

---

## 1. 역할 분담

### 시나리오 A — 4인이 각자 다른 영역을 전담

| 역할 | 담당 영역 | 세부 업무 |
|---|---|---|
| **본인** (팀장 / 백엔드 코어) | 인증·공통 인프라 | 카카오 OAuth 연동, JWT 발급/검증/갱신, 공통 응답·에러 포맷(`{ error: { code, message } }`), Flask 프로젝트 골격(`app/__init__.py`, `config.py`, `extensions.py`), 전체 코드리뷰 |
| **팀원 A** (백엔드) | DB/모델링 + Region·Property API | ERD 10개 테이블 SQLAlchemy 모델, Flask-Migrate 마이그레이션, `GET /regions`, `GET /regions/{id}/properties`(정렬/필터/페이지네이션), `GET /properties/{id}`, `GET /properties/{id}/amenities` |
| **팀원 B** (백엔드) | 크롤러 + Route/Favorite/Compare API | 네이버 부동산 크롤러(평택/성환/직산/두정 4개 지역), `property`/`property_complex` 적재, `GET /properties/{id}/route`(4개 이동수단 시간·거리 계산), `favorite`/`compare` API |
| **팀원 C** (프론트) | AI 활용 프론트엔드 | AI 코드생성 도구(Cursor/v0/Claude 등)로 11개 화면 생성, React Router 구성 및 바텀네비 실연결, API 연동, 전역 상태관리(토큰/선택지역/정렬 등) |

### 시나리오 B — 본인이 인증(전반부) + 프론트엔드 AI 총괄(후반부)까지 겸임

| 역할 | 담당 영역 | 비고 |
|---|---|---|
| **본인** | 인증/공통포맷(W1~5) → 프론트엔드 AI 총괄(W6~12) | 인증 파트를 조기(5주 내) 완료·안정화한 뒤, 6주차부터 프론트 조립·API 연동을 주도 |
| **팀원 A** | DB/모델링 + Region/Property/Amenity API | 시나리오 A와 동일 범위, 단독 진행(협업할 인증 담당이 이후 프론트로 이동하므로 API 계약은 1~2주차에 본인과 미리 확정) |
| **팀원 B** | 크롤러 + Route API | 시나리오 A와 동일 |
| **팀원 C** | Favorite/Compare/MyPage API(W1~6) → 프론트 보조(W6~12) | 백엔드 담당 API를 6주차까지 마감한 뒤, 본인과 함께 프론트 연동·다듬기 합류 (2인 체제) |

**차이 요약**: 시나리오 A는 프론트를 W2~W9에 걸쳐 팀원 C 1인이 전담. 시나리오 B는 본인이 백엔드 인증에 먼저 집중하느라 프론트 착수가 W6로 늦어지는 대신, W6부터 본인+팀원C 2인이 붙어 남은 6주(W6~11)에 압축 진행한다. **팀원 3명 모두 처음 정해진 역할에 강한 선호가 없다면 시나리오 A를 권장** — 프론트 담당이 처음부터 화면을 붙잡고 있어야 AI 생성 결과의 일관성(디자인 시스템, 상태관리 패턴)을 유지하기 쉽고, 본인의 인증 전문성이 팀원들 온보딩(코드리뷰)에 계속 쓰일 수 있기 때문. 실제 팀원 성향(프론트 관심 있는 사람이 있는지)을 보고 최종 선택할 것.

---

## 2. 주차별 계획 (12주)

### 2-1. 공통 로드맵 개요

| 주차 | 마일스톤 |
|---|---|
| W1 | 킥오프 · 환경 세팅 · 명세 확정 |
| W2 | 인증/DB/크롤러/프론트 뼈대 착수 |
| W3 | Region·Property API + 크롤러 4개 지역 완료 |
| W4 | Amenity·Route API |
| W5 | Favorite·Compare API |
| W6 | MyPage API 마무리 + 백엔드 기능 동결 + 중간 점검 |
| W7 | 연동① 인증 + 지역선택 + 매물리스트 |
| W8 | 연동② 매물상세 + 경로상세 + 매물비교 |
| W9 | 연동③ 즐겨찾기 + 마이페이지 + 바텀네비 라우팅 |
| W10 | 미설계 상태(빈 상태/에러 상태) 보강, 리뷰/커뮤니티 연동 |
| W11 | 통합 테스트 · 버그 픽스 |
| W12 | 최종 QA · 문서화 · 발표 준비 |

### 2-2. 시나리오 A 상세 (본인 / 팀원A / 팀원B / 팀원C)

**W1 — 킥오프/세팅**
- 전체: Figma 11개 화면 재검토, ERD/API 명세 최종 확정(변경사항 있으면 스펙 문서 수정), GitHub 저장소·브랜치 전략·이슈보드 세팅
- 본인: 카카오 개발자 계정/앱 등록 및 REST API 키 발급(승인 지연 가능성 있어 최우선 처리), Flask 프로젝트 스캐폴딩(`backend/`)
- 팀원A: Flask/SQLAlchemy 공식 튜토리얼 학습, MySQL 로컬 설치
- 팀원B: 네이버 부동산 페이지 구조 분석(크롤링 대상 URL/HTML 구조 파악), Python 크롤링 라이브러리(BeautifulSoup/Playwright) 학습
- 팀원C: React 기초 학습, AI 코드생성 도구 사용법 학습, Figma 화면별 컴포넌트 구조 정리

**W2 — 기본 골격**
- 본인: 카카오 로그인(`POST /auth/kakao/login`) 구현, JWT 발급/검증 미들웨어, `/auth/logout`, `/auth/token/refresh`
- 팀원A: SQLAlchemy 모델 10개 테이블 전체 작성 + Flask-Migrate 마이그레이션, `region` 더미데이터 시딩
- 팀원B: 크롤러 프로토타입(성환 1개 지역 매물 목록 수집 성공)
- 팀원C: React 프로젝트 세팅(Vite), 라우터 구조 설계, 11개 화면 빈 컴포넌트 생성, AI로 Landing/지역선택 화면 1차 생성

**W3 — Region·Property API + 크롤러 완료**
- 본인: 인증 통합 테스트, 전역 에러 핸들러(`errors.py`) 작성 및 팀 공유(Authorization 데코레이터 제공)
- 팀원A: `GET /regions`, `GET /regions/{id}/properties`(정렬 distance/transit/price, 페이지네이션) 구현
- 팀원B: 크롤러 4개 지역(평택/성환/직산/두정) 전체 수집 완료, 배치 스크립트화, `GET /properties/{id}` 구현
- 팀원C: AI로 지역매물 리스트/매물상세 화면 생성 (mock 데이터로 우선 붙임)

**W4 — Amenity·Route API**
- 본인: 인증 가드(Bearer 토큰 필요 API) 전체 적용 확인, 팀원 코드 1차 리뷰
- 팀원A: `GET /properties/{id}/amenities`(편의점/카페/헬스장 카테고리 집계 포함) 구현
- 팀원B: `GET /properties/{id}/route`(도보/버스/자전거/택시, 직선거리·추정 기반. polyline은 null 유지) 구현
- 팀원C: AI로 경로상세/매물비교선택 화면 생성

**W5 — Favorite·Compare API**
- 본인: JWT 갱신 흐름 프론트 연동 가이드 문서 작성
- 팀원A: `GET/POST /favorites`, `DELETE /favorites/{propertyId}` 구현
- 팀원B: `POST /properties/compare`(2~4개, bestFields 계산 로직) 구현
- 팀원C: AI로 매물비교테이블/즐겨찾기/마이페이지 화면 생성

**W6 — MyPage 마무리 + 중간 점검**
- 본인+팀원A/B 분담: `GET /users/me`, `GET /users/me/recent-viewed`(조회 시 upsert), `GET /users/me/compare-history`, 알림설정 `GET/PATCH`(저장만, 실제 발송 없음)
- 전체: 백엔드 API 1차 기능 동결, Postman/Thunder Client로 전체 엔드포인트 점검, 중간 회고
- 팀원C: 프론트 11개 화면 1차 완성(AI 생성 기준), mock 데이터로 전체 플로우 클릭 가능하게

**W7 — 연동① 인증/지역선택/매물리스트**
- 팀원C 주도, 본인 지원: 카카오 로그인 연동, 토큰 저장/재발급 흐름, 지역선택(지도·리스트) + 매물리스트 화면을 실제 API로 교체
- 팀원A/B: 연동 중 발견되는 자기 담당 API 버그 수정

**W8 — 연동② 매물상세/경로상세/매물비교**
- 매물상세, 경로상세(4개 이동수단 탭), 매물비교선택·테이블 화면 실 API 연동
- 본인: 각 화면 API 계약(응답 스키마) 불일치 여부 점검

**W9 — 연동③ 즐겨찾기/마이페이지/바텀네비**
- 즐겨찾기, 마이페이지 화면 연동. 바텀네비(홈/즐겨찾기/마이페이지) 실제 라우팅 연결(현재 Figma는 시각적 목업뿐)
- 크롤링 데이터 최신화(팀원B), region 카운트 등 정합성 점검(팀원A)

**W10 — 미설계 상태 보강**
- 03 필터 결과 0건일 때 빈 상태, 07 특정 이동수단 경로 없음 상태, 08 비교 선택 2개 미만/4개 초과 제한 UI, 10 최근 본 매물/비교 기록 없음 빈 상태 추가
- "최근 본 매물" 화면 신규 제작(Figma에 없음, 즐겨찾기 화면과 유사한 그리드로 구성) — 팀원C
- "집주인에게 문의하기" 버튼은 MVP 범위 외이므로 클릭 시 비활성/안내 처리만
- 리뷰(평점 표시/작성) API 연동 및 커뮤니티(목록/상세/글쓰기·댓글) API 연동

**W11 — 통합 테스트 · 버그 픽스**
- 전체: 서로 다른 사람이 만든 기능을 교차 테스트, 버그 트래킹 및 수정
- 로컬 시연 시나리오(신규 유저 가입→지역선택→매물비교→즐겨찾기 순서 등) 스크립트 작성

**W12 — 최종 QA · 문서화 · 발표 준비**
- README 정리(실행 방법, 환경변수, 시연 순서), 발표자료 제작, 리허설, 팀 회고

### 2-3. 시나리오 B에서 달라지는 부분

주차별 세부 업무는 위 시나리오 A와 대부분 동일하되, 아래만 다르게 적용:

- **W1~5**: 팀원C는 프론트 착수 없이 백엔드(Favorite/Compare/MyPage API)에 집중. 프론트 학습(React/AI 툴)은 본인이 W1에 잠깐 훑어보는 정도로 축소.
- **W6**: 본인은 인증 유지보수만 남기고 프론트로 전환 시작. 팀원C는 자기 API를 마감하는 대로(늦어도 W6 중) 프론트 합류.
- **W7~9 (연동, 3주)**: 본인 + 팀원C 2인 체제로 진행. 화면 분담 예: 본인이 인증/지역선택/매물상세/경로상세, 팀원C가 매물리스트/비교/즐겨찾기/마이페이지/바텀네비. 2인이므로 매주 화면군을 나눠 병렬 진행 가능해 시나리오 A와 동일한 3주 내 완료 목표 유지.
- **W10~12**: 동일(전체 참여).

---

## 3. 주요 리스크 및 대응

| 리스크 | 대응 |
|---|---|
| 카카오 개발자 앱 등록/키 발급 지연 | 1주차 최우선 처리, 승인 대기 중에는 임시 카카오 계정으로 개발용 앱 먼저 생성 |
| 네이버 부동산 크롤링 구조 변경/차단 | 초기에 HTML 샘플을 로컬에 저장해두고 파싱 로직 개발, 크롤링 실패 시 더미데이터로 즉시 전환할 수 있게 시딩 스크립트 별도 유지 |
| 3명의 개발 학습곡선 | 매주 계획에 학습 시간 명시적 배정, 본인의 코드리뷰를 온보딩 수단으로 활용, 막히면 주간 회의 때 바로 페어 프로그래밍 |
| AI 생성 프론트 코드 품질/일관성 편차 | 화면별 완료 기준(라우팅 연결/API 연동/반응형/에러 및 빈 상태 처리)을 체크리스트로 관리, 병합 전 실행 확인 필수 |
| 12주로 압축된 일정(중간고사 등 학사일정과 겹칠 수 있음) | 실제 팀원 시험기간을 확인해 W6(중간 점검) 전후로 유동적 버퍼 배치 |
| MVP 범위 후반 추가(리뷰/커뮤니티)로 개발 물량 증가 | W10 버퍼를 리뷰/커뮤니티 연동에 우선 배정, 필요 시 W11 통합테스트 범위 축소 |

---

## 4. 발표 전 최종 체크리스트 (MVP 포함 범위)

- [ ] 카카오 로그인/로그아웃
- [ ] 지역 목록(지도형/리스트형 토글) + 지역별 매물 수 표시
- [ ] 지역 매물 리스트: 정렬(거리/교통/가격), 편의시설 필터
- [ ] 매물 상세: 사진, 가격, 주변 편의시설, 4개 이동수단별 소요시간
- [ ] 경로 상세 전체화면(도보/버스/자전거/택시)
- [ ] 매물 비교(최대 4개 선택 → 비교 테이블, 항목별 최적값 표시)
- [ ] 즐겨찾기(추가/삭제/목록/지역필터/정렬/빈 상태)
- [ ] 마이페이지(프로필, 즐겨찾기·최근 본 매물·비교기록 진입점, 로그아웃)
- [ ] 매물 리뷰(평점 표시/작성)
- [ ] 커뮤니티(게시글 목록/상세/글쓰기, 댓글)
- [ ] 바텀네비 실제 라우팅 동작(홈/즐겨찾기/커뮤니티/마이페이지)
- [ ] 네이버 부동산 실 데이터 기반 4개 지역 매물 크롤링 결과 반영
- [ ] 로컬 환경에서 전체 플로우 시연 가능 여부 최종 확인
