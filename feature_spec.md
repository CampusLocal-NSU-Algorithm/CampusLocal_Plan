# 기능 명세서 (Feature Spec)

> 각 화면은 Figma 프레임명을 화면 ID로 사용한다. "트리거"는 해당 액션이 발생했을 때의 화면 전환/상태 변화를 의미한다.

---

## 01_Landing_로그인전

**진입 조건**: 비로그인 상태 (앱 최초 진입 시 기본 화면)

**표시 데이터**
- 서비스 소개 문구, 기능 소개 카드 3종(실시간 매물 지도 / 학교까지 소요시간 자동 계산 / 매물 비교) — 각 카드에 실제 화면 미리보기 이미지 포함
- 자취방 구하는 꿀팁 카드 목록 (캐러셀)

**사용자 액션 / 트리거**
| 액션 | 트리거 |
|---|---|
| 카카오 로그인 버튼 클릭 | 카카오 OAuth 인가 → 로그인 성공 시 `02 Main 지역선택`으로 이동 |
| 꿀팁 캐러셀 좌우 스와이프/화살표 클릭 | 캐러셀 내 카드 이동 (화면 전환 없음) |

**예외/빈 상태**: 카카오 로그인 실패 시 에러 메시지 노출 후 랜딩에 머무름 (별도 화면 없음)

---

## 02_Main_지역선택 (지도형) / 02b_Main_지역선택_리스트뷰 (리스트형)

**진입 조건**: 로그인 완료 직후, 또는 헤더 로고·"홈" 탭 클릭 시

**표시 데이터**
- 지역 4곳(평택/성환/직산/두정)의 이름, 지역 설명, 매물 개수
- (지도형) 남서울대학교 중심 랜드마크 마커 + 지역별 핀(매물 개수에 비례한 크기/색상), 학교로부터의 상대적 방향·거리
- (리스트형) 지역별 요약 카드 (매물 개수 내림차순 정렬)
- 바텀시트: 선택된 지역명, 소재지 설명, 매물 개수

**사용자 액션 / 트리거**
| 액션 | 트리거 |
|---|---|
| 지도 핀 또는 리스트 행 클릭 | 해당 지역 선택 상태로 변경. 지도형에서는 하단 바텀시트가 슬라이드업되며 선택 지역 정보로 갱신 |
| 바텀시트 "매물 보기" 버튼 클릭 | `03 지역매물 리스트+지도`로 이동 (선택된 지역 기준) |
| 리스트뷰 행 클릭 | `03 지역매물 리스트+지도`로 바로 이동 |
| 지도/리스트 토글 클릭 | 02(지도형) ↔ 02b(리스트형) 화면 전환 |
| 헤더 하트 아이콘 클릭 | `favorites` 또는 `favorites-empty`로 이동 (즐겨찾기 존재 여부에 따라 분기) |
| 헤더 프로필 아바타 클릭 | `10 마이페이지`로 이동 |

**예외/빈 상태**: 해당 없음 (지역 데이터는 고정 4개)

---

## 03_지역매물_리스트+지도

**진입 조건**: 02/02b에서 지역 선택 후 진입 (선택된 지역 ID를 파라미터로 전달)

**표시 데이터**
- 선택 지역명 + 매물 총 건수
- 매물 리스트: 썸네일, 원룸/투룸 뱃지, 월세 가격, 학교까지 도보시간, 주소, 정렬 순위 번호, 즐겨찾기 여부
- 지도: 학교 마커, 매물 가격 마커(선택/즐겨찾기 상태 구분), 다른 지역으로 바로 이동하는 플로팅 지역 전환 버튼

**사용자 액션 / 트리거**
| 액션 | 트리거 |
|---|---|
| 정렬 탭(거리순/교통순/가격순) 클릭 | 매물 리스트 재정렬, 지도 마커에도 순위 번호 반영, 선택된 정렬 탭 하이라이트 |
| 편의시설 필터 칩 클릭 | On/Off 토글 — 지도 위 편의시설(편의점/카페/헬스장 등) 마커 표시 여부 전환 |
| 매물 카드 또는 지도 가격 마커 클릭 | `04 매물상세`로 이동 (선택 매물 ID 전달) |
| 매물 카드의 하트 아이콘 클릭 | 즐겨찾기 등록/해제 토글 (화면 전환 없음, 아이콘 상태만 변경) |
| "이 지역 매물 순위 비교" 클릭 | `08 매물비교 선택`으로 이동 (현재 지역 매물 목록 전달) |
| 플로팅 지역 전환 버튼 클릭 | 동일 화면 내에서 다른 지역 데이터로 갱신 |
| 지도 확대/축소(+/−) 클릭 | 지도 줌 레벨 변경 (화면 전환 없음) |

**예외/빈 상태**: 필터 적용 결과 매물이 0건일 경우 "조건에 맞는 매물이 없어요" 형태의 빈 리스트 안내 필요 (현재 디자인 미반영, 구현 시 추가)

---

## 04_매물상세

**진입 조건**: 03에서 매물 카드 또는 지도 마커 클릭 시 (매물 ID 필수)

**표시 데이터**
- 매물 사진(다중, 사진 카운터 "1/8" 형태), 거래유형/방구조 뱃지, 매물명, 월세·관리비, 주소
- 주변 편의시설 미니맵(편의점/카페/헬스장 마커 + 개수 범례)
- 학교까지 이동수단별(도보/버스/자전거/택시) 소요시간 카드 4종
- 즐겨찾기 여부, 길찾기 팝업(선택 이동수단의 총 소요시간/거리, 경로 요약)

**사용자 액션 / 트리거**
| 액션 | 트리거 |
|---|---|
| 이동수단 카드 클릭 | 길찾기 팝업 표시 또는 `07 경로상세 전체화면`으로 이동 |
| 닫기(X) 클릭 | `03 지역매물 리스트+지도`로 복귀 |
| 즐겨찾기 버튼 클릭 | 즐겨찾기 등록/해제 토글 |
| "집주인에게 문의하기" 버튼 클릭 | MVP 범위 외 (버튼만 존재, 실제 문의 기능은 추후 고도화 시 고려) |
| 패널 스크롤 | 세로 스크롤로 하단 콘텐츠 탐색 |

**예외/빈 상태**: 해당 없음

**참고**: 네이버 부동산 크롤링 시 함께 수집되는 단지정보(건축년도/세대수/주차대수/난방방식 등)는 현재 화면에 미반영. API/DB는 대비해서 설계하되, 프론트 UI는 추후 별도 반영.

---

## 07_경로상세_전체화면

**진입 조건**: 04에서 이동수단 카드 클릭 시 (매물 ID + 이동수단 모드 전달)

**표시 데이터**
- 이동수단 탭(도보/버스/자전거/택시)별 소요시간
- 큰 지도 위 출발지(매물)-도착지(학교) 경로선
- 선택된 이동수단의 총 소요시간·거리, 도착 예정 시각, 단계별 경로 안내

**사용자 액션 / 트리거**
| 액션 | 트리거 |
|---|---|
| 이동수단 탭 전환 | 해당 이동수단 기준으로 경로/소요시간 재조회 및 갱신 |
| "매물 상세로 돌아가기" 버튼 클릭 | `04 매물상세`로 복귀 |

**예외/빈 상태**: 해당 이동수단으로 경로를 찾을 수 없는 경우(예: 택시 데이터 없음) 안내 필요 (현재 디자인 미반영, 구현 시 추가)

---

## 08_매물비교_선택

**진입 조건**: 03에서 "이 지역 매물 순위 비교" 클릭 시

**표시 데이터**: 현재 지역의 매물 그리드(썸네일, 이름, 가격, 선택 체크박스 상태)

**사용자 액션 / 트리거**
| 액션 | 트리거 |
|---|---|
| 매물 카드 클릭 | 비교 대상 선택/해제 토글 (최대 4개 제한) |
| "선택한 N개 비교하기" 버튼 클릭 | `09 매물비교 테이블`로 이동 (선택된 매물 ID 목록 전달) |

**예외/빈 상태**: 2개 미만 선택 시 비교 버튼 비활성화 필요 (현재 디자인 미반영, 구현 시 추가). 4개 초과 선택 시도 시 추가 선택 불가 안내 필요

---

## 09_매물비교_테이블

**진입 조건**: 08에서 "비교하기" 클릭 시 (선택 매물 ID 목록 필수)

**표시 데이터**: 선택 매물별 사진/이름 헤더 + 속성 비교 행(가격/관리비/방구조/학교까지 도보/학교까지 버스/주변 편의시설/즐겨찾기 여부), 속성별 최고값 하이라이트(★)

**사용자 액션 / 트리거**
| 액션 | 트리거 |
|---|---|
| 닫기(X) 클릭 | 이전 화면(03 또는 10)으로 복귀 |
| 비교 기준 정렬(가격순/거리순/교통순) 전환 | 비교 대상 매물의 정렬 순서만 변경 (화면 전환 없음) |

**예외/빈 상태**: 해당 없음

---

## favorites / favorites-empty (즐겨찾기)

**진입 조건**: 헤더 하트 아이콘, 10 마이페이지의 "즐겨찾기한 매물" 메뉴 클릭 시

**표시 데이터**
- (favorites) 즐겨찾기 매물 건수, 지역 필터 칩, 정렬 탭(거리순/교통순/가격순), 매물 카드 그리드
- (favorites-empty) 안내 문구("아직 찜한 매물이 없어요") + "매물 둘러보기" 버튼

**사용자 액션 / 트리거**
| 액션 | 트리거 |
|---|---|
| 지역 필터 칩 클릭 | 해당 지역 매물만 필터링 |
| 정렬 탭 클릭 | 리스트 재정렬 |
| 매물 카드 클릭 | `04 매물상세`로 이동 |
| 매물 카드 하트 아이콘 클릭 | 즐겨찾기 해제 (목록에서 제거) |
| (빈 상태) "매물 둘러보기" 버튼 클릭 | `02 Main 지역선택`으로 이동 |

**예외/빈 상태**: 즐겨찾기 매물이 0건이면 `favorites-empty` 화면으로 분기

---

## 10_마이페이지

**진입 조건**: 헤더 프로필 아바타 클릭 시

**표시 데이터**: 프로필(이름, 학과, 카카오 계정 연동 여부), 메뉴 목록(즐겨찾기한 매물/최근 본 매물/비교했던 매물/알림 설정/로그아웃)

**사용자 액션 / 트리거**
| 액션 | 트리거 |
|---|---|
| "즐겨찾기한 매물" 클릭 | `favorites`(또는 empty)로 이동 |
| "최근 본 매물" 클릭 | 최근 본 매물 목록 화면으로 이동 (현재 Figma 화면 미제작 — 구현 시 favorites와 유사한 그리드로 신규 제작 필요) |
| "비교했던 매물" 클릭 | `09 매물비교 테이블`로 이동 (가장 최근 비교 이력 기준) |
| "알림 설정" 클릭 | 알림 설정 화면/모달 (MVP 범위 외, 추후 고도화 시 고려) |
| "로그아웃" 클릭 | 로그아웃 처리 후 `01 Landing`으로 이동 |

**예외/빈 상태**: 최근 본 매물/비교 이력이 없을 경우 각각 빈 상태 안내 필요 (현재 디자인 미반영, 구현 시 추가)

---

## 공통 컴포넌트

- **하단 네비게이션 바** (홈/즐겨찾기/마이페이지): favorites, favorites-empty, 10_마이페이지 화면 하단에 표시. 현재 Figma 프로토타입에서는 시각 요소만 존재하며 실제 화면 전환은 연결되어 있지 않음 — 프론트 구현 시 라우팅 필요.
